[package]
name = "avatar-input"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "avatar_input"
path = "src/lib.rs"

[dependencies]
glam.workspace = true
rosc = { version = "0.11", optional = true }

[features]
default = []
# Real OSC backend (VMC/tracker-style transforms over UDP). Pure-Rust, no system deps.
osc = ["dep:rosc"]
