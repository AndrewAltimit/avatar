[package]
name = "avatar-unity-yaml"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "avatar_unity_yaml"
path = "src/lib.rs"

[dependencies]
anyhow.workspace = true
yaml-rust2.workspace = true

[dev-dependencies]
# Reaches the committed fixture corpus (`fixtures/`) so the surgical-edit round-trip is exercised
# against real Unity assets, not only embedded snippets.
avatar-testkit = { path = "../testkit" }
