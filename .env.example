# UID/GID for the Rust build container (docker/rust.Dockerfile).
# Defaults to 1000:1000 if unset; override to match a non-default host user:
#   USER_ID=$(id -u)
#   GROUP_ID=$(id -g)
# then: docker compose build rust
USER_ID=1000
GROUP_ID=1000
